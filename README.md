# 🎨 Excalidraw Rooms

แอปพลิเคชัน Excalidraw ที่สามารถสร้างห้องแยกกันผ่าน URL ได้ โดยแต่ละห้องจะมีพื้นที่วาดภาพแยกกัน พร้อมระบบบันทึกข้อมูลอัตโนมัติ

## ✨ คุณสมบัติ

- 🏠 **ระบบห้อง**: สร้างห้องแยกกันสำหรับการวาดภาพ
- 🔗 **URL-based**: เข้าถึงห้องผ่าน URL เช่น `/room/meeting-room-1`
- 💾 **Auto Save**: บันทึกข้อมูลการวาดอัตโนมัติใน Local Storage
- 📱 **Responsive**: รองรับการใช้งานบนมือถือ
- 🎯 **Real-time**: การวาดภาพแบบ Real-time ในแต่ละห้อง
- ⏰ **Save Status**: แสดงสถานะการบันทึกแบบ Real-time
- 📅 **Timestamp**: แสดงเวลาบันทึกล่าสุดของแต่ละห้อง

## 🚀 การติดตั้ง

1. Clone โปรเจค:
```bash
git clone <repository-url>
cd excalidraw
```

2. ติดตั้ง dependencies:
```bash
npm install
```

3. รันแอปพลิเคชัน:
```bash
npm run dev
```

4. เปิดเบราว์เซอร์ไปที่ `http://localhost:5173`

## 📖 วิธีการใช้งาน

### 1. สร้างห้องใหม่
- ไปที่หน้าหลัก
- ใส่ชื่อห้องในช่อง "สร้างห้องใหม่"
- กดปุ่ม "สร้างห้อง" หรือกด Enter

### 2. เข้าห้อง
- คลิกปุ่ม "เข้าห้อง" ที่ห้องที่ต้องการ
- หรือพิมพ์ URL โดยตรง: `http://localhost:5173/room/[ชื่อห้อง]`

### 3. การบันทึกข้อมูล
- **อัตโนมัติ**: ข้อมูลจะถูกบันทึกอัตโนมัติเมื่อมีการวาด
- **สถานะ**: ดูสถานะการบันทึกที่มุมบนซ้าย
  - ✅ บันทึกแล้ว
  - ⚠️ มีการเปลี่ยนแปลงที่ยังไม่ได้บันทึก
  - 🔄 กำลังบันทึก...
- **เวลา**: แสดงเวลาบันทึกล่าสุด

### 4. ลบห้อง
- คลิกปุ่ม "ลบ" ที่ห้องที่ต้องการลบ
- ข้อมูลห้องและข้อมูลการวาดจะถูกลบออกจาก Local Storage

### 5. กลับไปหน้าหลัก
- คลิกปุ่ม "← กลับไปหน้าหลัก" ที่มุมบนซ้ายของห้อง

## 💾 ระบบการบันทึกข้อมูล

### Local Storage Keys
- `excalidraw-rooms`: รายชื่อห้องทั้งหมด
- `excalidraw-room-[roomId]`: ข้อมูลการวาดของแต่ละห้อง
- `excalidraw-room-[roomId]-last-saved`: เวลาบันทึกล่าสุด

### ข้อมูลที่บันทึก
```typescript
interface DrawingData {
  elements: unknown[];        // องค์ประกอบการวาด
  appState: Record<string, unknown>;  // สถานะแอปพลิเคชัน
  timestamp: number;         // เวลาบันทึก
}
```

### การกู้คืนข้อมูล
- ข้อมูลจะถูกโหลดอัตโนมัติเมื่อเข้าห้อง
- หากไม่มีข้อมูลที่บันทึกไว้ จะเริ่มต้นด้วยกระดานว่าง

## 🛠️ เทคโนโลยีที่ใช้

- **React 19**: Frontend framework
- **TypeScript**: Type safety
- **React Router**: URL-based routing
- **Excalidraw**: Drawing library
- **Vite**: Build tool
- **Local Storage**: Data persistence

## 📁 โครงสร้างโปรเจค

```
excalidraw/
├── src/
│   ├── App.tsx          # Main application component
│   ├── App.css          # Styles
│   ├── main.tsx         # Entry point
│   └── index.css        # Global styles
├── public/              # Static assets
├── package.json         # Dependencies
└── README.md           # This file
```

## 🔧 การปรับแต่ง

### เพิ่มคุณสมบัติใหม่
คุณสามารถเพิ่มคุณสมบัติใหม่ได้โดยการแก้ไขไฟล์ `src/App.tsx`:

- เพิ่ม props ให้กับ Excalidraw component
- เพิ่มฟีเจอร์การแชร์ห้อง
- เพิ่มระบบ authentication
- เพิ่มการบันทึกภาพ
- เพิ่มการ export/import ข้อมูล

### ปรับแต่ง UI
แก้ไขไฟล์ `src/App.css` เพื่อปรับแต่ง:
- สี
- ฟอนต์
- Layout
- Animations

### ปรับแต่งการบันทึก
แก้ไขฟังก์ชัน `saveDrawingData` และ `loadDrawingData` ใน `src/App.tsx`:
- เปลี่ยนจาก Local Storage เป็น Database
- เพิ่มการบีบอัดข้อมูล
- เพิ่มการเข้ารหัสข้อมูล

## 🌐 การ Deploy

### Vercel
1. Push โค้ดไปยัง GitHub
2. เชื่อมต่อกับ Vercel
3. Deploy อัตโนมัติ

### Netlify
1. Push โค้ดไปยัง GitHub
2. เชื่อมต่อกับ Netlify
3. Build command: `npm run build`
4. Publish directory: `dist`

## 🤝 การมีส่วนร่วม

1. Fork โปรเจค
2. สร้าง feature branch
3. Commit การเปลี่ยนแปลง
4. Push ไปยัง branch
5. สร้าง Pull Request

## 📄 License

MIT License - ดูไฟล์ LICENSE สำหรับรายละเอียด

## 🆘 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

1. **Excalidraw ไม่โหลด**
   - ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
   - ลองรีเฟรชหน้า

2. **ห้องไม่แสดง**
   - ตรวจสอบ Local Storage
   - ลองล้าง cache ของเบราว์เซอร์

3. **URL ไม่ทำงาน**
   - ตรวจสอบ React Router setup
   - ตรวจสอบ server configuration

4. **ข้อมูลไม่บันทึก**
   - ตรวจสอบ Local Storage quota
   - ลองล้าง cache ของเบราว์เซอร์
   - ตรวจสอบ Console สำหรับ error

5. **ข้อมูลหาย**
   - ตรวจสอบ Local Storage
   - ข้อมูลจะหายเมื่อล้าง cache หรือใช้ Private mode

## 🔮 แผนการพัฒนาต่อ

- [ ] ระบบการแชร์ห้อง
- [ ] การ export/import ข้อมูล
- [ ] ระบบ authentication
- [ ] การบันทึกภาพ
- [ ] การทำงานร่วมกันแบบ Real-time
- [ ] ระบบ backup ข้อมูล

## 📞 ติดต่อ

หากมีคำถามหรือปัญหา กรุณาสร้าง Issue ใน GitHub repository
